# Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Resend Email Configuration
RESEND_API_KEY=your_resend_api_key
# For development/testing - use Resend's default domain (only sends to your own email)
RESEND_FROM="onboarding@resend.dev"
# For production - use your verified domain
RESEND_FROM="Admissions <admissions@info.abru.fun>"

# Instructions:
# 1. Copy this file to .env.local in the root directory
# 2. Replace all placeholder values with your actual credentials
# 3. Make sure .env.local is in your .gitignore file (it should be by default)
# 4. Restart your development server after adding environment variables

# Supabase Setup:
# - Create a new project at https://supabase.com
# - Go to Settings > API to find your URL and keys
# - Run the SQL commands in supabase-setup.sql to create all tables and policies
# - Create admin users in Supabase Auth with these emails:
#   - thrissur@sla.com (password: set in Supabase Auth dashboard)
#   - chalakudy@sla.com (password: set in Supabase Auth dashboard)
#   - peravoor@sla.com (password: set in Supabase Auth dashboard)
#   - superadmin@sla.com (password: set in Supabase Auth dashboard)
# - Create a storage bucket named 'photos' with private access
# - Set up storage policies for authenticated uploads

# Admin Module Features:
# - Institution-specific admin authentication
# - Student registration management (CRUD operations)
# - Photo upload and download functionality
# - Print-friendly student forms
# - CSV export of student data
# - Audit logging for all changes
# - Role-based access control (admin vs super_admin)

# Strapi Configuration (for API Store)
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337/api
NEXT_PUBLIC_STRAPI_TOKEN=your_strapi_api_token

# Resend Setup:
# - Sign up at https://resend.com
# - Create an API key in your dashboard
# - Verify your domain for sending emails

# Strapi Setup:
# - Install and run Strapi backend
# - Create an API token in Strapi admin panel
# - Make sure your Strapi server is running on the specified URL
